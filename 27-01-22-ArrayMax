<html>
  <body>
    <script>
        var size = parseInt(prompt("Enter number of elements you want to add"));
        var arr = [];
        for(var i=0;i<size;i++){
           arr[i] = parseInt(prompt("Enter elements"));
        }
        
        for(var i=0;i<size;i++){
           for(var j=0;j<size;j++){
              if(arr[i]<arr[j]){
                 var temp = arr[i];
                 arr[i] = arr[j];
                 arr[j] = temp;
              }
           }
        }
        console.log(arr[size-1]);
    </script>
  </body>
  <h3>Take n inputs from the user. It has to be dynamic (multiple n inputs by user). i.e, I can enter 3 numbers as well as 30 numbers. It should give me the maximum of all of them. We will be using Array as the underlying data structure.</h3>
</html>
